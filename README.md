# Bloodline

Browser Oriented Tile Engine

## Running the Demo or Editor

If the project is cloned locally, you can run the demo by executing `npx serve .` in the project root directory and navigating to `http://localhost:3000/` in your web browser.
